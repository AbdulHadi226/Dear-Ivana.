function changeText() {
    var button = document.getElementById('love-button');
    if (button.innerText === 'Click this button') {
        button.innerText = "I love you so so so so much. Mwah 💋";
    } else {
        button.innerText = "Click this button";
    }
}
